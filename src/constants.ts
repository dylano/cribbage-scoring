export const WINNING_SCORE = 121;

// WinningPeg is 3rem across (see WinningPeg.module.css). The border's gap has
// to clear it, so it is the peg's diameter plus breathing room either side —
// raise PEG_CLEARANCE alone to give the peg more space.
export const WINNING_PEG_DIAMETER = 48;
export const WINNING_PEG_CLEARANCE = 6;
